# Scrapegoat – copy to .env and fill in
# Used by: main.py, start_redis_worker.py, pipeline (Chimera, Blueprint, etc.)

# --- Infrastructure ---
DATABASE_URL=postgresql://user:pass@host:5432/dbname
APP_DATABASE_URL=
REDIS_URL=redis://localhost:6379
APP_REDIS_URL=
APP_CELERY_BROKER_URL=redis://localhost:6379/1
APP_CELERY_RESULT_BACKEND=redis://localhost:6379/2

# --- Chimera Brain (Hive Mind: predict-path, store-pattern) ---
# At least one must be set for Chimera Deep Search station.
# HTTP (recommended): base URL, no path (e.g. https://chimera-brain-v1-production.up.railway.app)
CHIMERA_BRAIN_HTTP_URL=https://chimera-brain-v1-production.up.railway.app
# Or gRPC address; if set and contains 50051, HTTP URL is derived as :8080
# CHIMERA_BRAIN_ADDRESS=chimera-brain.railway.internal:50051

# --- API keys ---
OPENAI_API_KEY=
CENSUS_API_KEY=
RAPIDAPI_KEY=
TELNYX_API_KEY=
CAPSOLVER_API_KEY=

# --- Optional ---
PORT=8000
PYTHONUNBUFFERED=1
# Seconds to wait for Chimera Core to return results before retrying next provider. Default 120.
# If Chimera often times out, set 180–240 so people-search runs can finish.
CHIMERA_STATION_TIMEOUT=240
